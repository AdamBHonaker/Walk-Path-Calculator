# Production — set this to your Railway backend URL after deploying
# Commit this file — it contains no secrets
VITE_BACKEND_URL=https://fantastic-hope-walk-path-calculator.up.railway.app

# Map tile style URL — swap this to change tile providers without a code change
# Default (OpenFreeMap, community-run, no uptime guarantee):
VITE_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/liberty
